/* Loop through Ar5rays: forEach and map methods */
[2, 3, 4, 5].forEach(function (element) {
  console.log(element);
});

// Alternative with arrow function (way better)
[2, 3, 4, 5].forEach((number) => console.log(number));
